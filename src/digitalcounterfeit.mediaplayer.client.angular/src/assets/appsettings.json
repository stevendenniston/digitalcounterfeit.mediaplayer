{
    "env": {
        "name": "#{environment}"
    },    
    "mediaPlayerApiUrl": "#{MediaPlayerApi_Url}",
    "auth": {
        "domain": "#{Idp_Domain}",
        "clientId": "#{Idp_ClientId}",
        "audience": "#{Idp_Audience}",
        "scope": "#{Idp_Scope}"
    }
}