{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AudioFileLocation": "#{MediaPlayer_AudioFileLocation}",
  "AuthenticationDomain": "#{Authentication_Domain}",
  "AuthenticationAudience": "#{Authentication_Audience}",
  "AzureBlobAccountName": "#{Azure_BlobAccountName}",
  "AzureBlobAccountKey": "#{Azure_BlobAccountKey}",
  "CorsOrigin": "#{CorsOrigin}",
  "ConnectionString": "#{Database_ConnectionString}",
  "Version": "#{MediaPlayer_Version}"
}
