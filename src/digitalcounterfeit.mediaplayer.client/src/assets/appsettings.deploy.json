{
    "env": {
        "name": "#{environment}"
    },    
    "mediaPlayerApiUrl": "#{MediaPlayerApi_Url}",
    "idpSettings": {
        "authority": "#{Idp_Authority}",
        "client_id": "#{Idp_ClientId}",
        "redirect_uri": "#{Idp_RedirectUri}",
        "scope": "#{Idp_Scope}",
        "response_type": "#{Idp_ResponseType}",
        "post_logout_redirect_uri": "#{Idp_PostLogoutRedirectUri}"
    }
}